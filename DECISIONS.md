# DECISIONS — 무엇을 왜 이렇게 정했는가

각 결정은 대안과 함께 적는다. 나중에 바꿀 때 근거를 다시 만들지 않기 위해서다.

## D1. 실행 엔진 — Claude Agent SDK (Python)

| 후보 | 장점 | 단점 | 판단 |
|---|---|---|---|
| **Claude Agent SDK (Python)** | 인프로세스 MCP 도구를 스키마와 함께 정의할 수 있다. `can_use_tool`로 권한 게이트를 코드에 둔다. `resume`으로 세션을 잇는다 | 비동기 코드가 서버 스레드와 섞인다 | **채택** |
| Claude CLI 서브프로세스 | 설치·인증이 이미 끝나 있어 가장 빠르다 | 도구 스키마 통제가 약하고, 질문 대기를 `need_input` 규약으로 직접 만들어야 한다 | 미채택 |
| OpenCode CLI | — | 계정·검증 이력이 없다 | 미채택 |

**결정 이유**: 과제 평가 2번(도구를 입출력 스키마와 함께 연결)과 3번(에이전트 루프·상태 관리·종료 조건)이 SDK에서 훨씬 직접적으로 충족된다. 도구를 파이썬 함수로 두면 실패 처리와 권한 검사를 같은 자리에 쓸 수 있다.

**비동기 처리 방식**: 실행 하나당 스레드 하나를 만들고 그 안에서 `asyncio.run()`을 돈다. 사람의 답을 기다리는 지점은 `threading.Event`를 `asyncio.to_thread`로 await 한다. 서버(HTTP 스레드)가 `set()` 하면 에이전트가 이어진다.

## D2. 7단계를 한 세션으로 잇는다 (단계마다 새 호출 X)

단계별로 프롬프트를 끊어 보내면 워크플로에 가깝고, 한 세션으로 이으면 에이전트 루프에 가깝다. 과제가 요구하는 것은 후자(계획→도구 호출→관찰→다음 행동)이므로 **한 세션 연속 실행**을 택했다.

대신 관찰 가능성을 위해 `set_stage` 도구를 두어 에이전트가 스스로 현재 단계를 보고하게 했다. 화면의 7단계 보드는 이 값을 읽는다.

## D3. 금칙어 검사는 LLM이 아니라 규칙 사전으로

`check_compliance`를 LLM 판단에 맡기면 같은 문장에 대해 결과가 흔들린다. 규제는 흔들리면 안 되는 영역이라 `knowledge/rules.json`의 정규식 사전 기반 **결정론적 검사**로 만들었다.

- 장점: 재현 가능, 감사 가능(어떤 규칙 몇 개를 적용했는지 화면에 표시), 비용 0
- 한계: 문맥을 못 본다. "치료실에서도 쓰는"처럼 형태만 걸리는 오탐이 난다 → `severity: warn`과 `block`을 나누고, 오탐은 사람이 H3 지점에서 확인한다

## D4. 사람 개입은 5곳으로 제한

전 단계에 승인 버튼을 두면 자동화의 의미가 없고, 없애면 신뢰할 수 없다. **결과가 크게 갈리는 지점**만 남겼다: 포지셔닝(H1), 스토리보드(H2), 컴플라이언스 경고(H3), 이미지 생성 승인(H4), 최종 승인(H5).

입력 폼에서 이미 받은 값(타겟·판매유형·가격)은 다시 묻지 않도록 시스템 프롬프트에 명시했다. 강의 4절의 '이미 답한 질문 반복 금지' 요구를 이 방식으로 처리했다.

## D5. `generate_image`은 `allowed_tools`에서 뺀다

SDK는 `allowed_tools`에 든 도구를 자동 승인하고 `can_use_tool` 콜백을 건너뛴다(`CanUseToolShadowedWarning`으로 경고해준다). 과금·비가역 도구가 자동 승인되면 권한 최소화 원칙이 깨지므로, `generate_image`만 목록에서 제외해 콜백을 반드시 거치게 했다.

실제 승인 대기는 도구 본체에서 처리한다 — `pending_approval`을 state.json에 쓰고 화면에 예상 비용을 띄운 뒤 사용자의 승인 이벤트를 기다린다. 승인 없이는 호출이 성립하지 않는다.

## D6. 쓰기 권한은 실행 폴더 안으로만

`save_artifact`는 경로를 정규화한 뒤 `runs/<run_id>/artifacts/` 접두사를 검사하고, 심볼릭 링크를 거부한다. 위반 시도는 `trace.jsonl`에 `security` 종류로 남긴다.

또한 `Bash`·`Write`·`Edit`은 `disallowed_tools`로 막았다. 에이전트가 파일을 만들 통로는 `save_artifact` 하나뿐이다.

## D7. 조사 도구는 별도 세션으로 분리 (리서처 역할)

`search_market`은 본 실행과 다른 SDK 세션을 열어 웹 검색을 수행하고, 결과를 우리 스키마(JSON)로 정규화해서 돌려준다.

- 이유: 본 세션의 맥락이 검색 원문으로 오염되는 것을 막는다. 조사 실패가 본 실행을 죽이지 않는다
- 부수 효과: 역할을 나눈 다중 에이전트 구조가 된다 (조사 → 작성)
- 비용 통제: 조사 세션은 `max_turns=8`, `max_budget_usd=0.5`로 따로 묶는다

## D8. 배포는 GitHub Pages 데모 + 로컬 실서비스

실서버(Render 등)에 올리면 링크만으로 진짜 실행이 가능하지만, API 키·과금·빌드 문제로 마감(2026-09-09 17:00) 안에 실패할 위험이 컸다.

**택한 것**: 실제 실행의 트레이스·질문·산출물을 담은 정적 데모를 Pages로 배포하고, 실제 에이전트 실행은 로컬에서 한다. 배포된 페이지가 실행 엔진에 연결되지 않는다는 점을 README에 명시한다.

**나중에 바꿀 조건**: 사용자가 여러 명 되거나, 외부에 시연할 일이 생기면 실서버로 옮긴다.

## D9. 상품 1건은 실제 업무 건으로 — 요가매트 렌탈

데모용 가짜 상품 대신 핸드오프 큐 1순위인 실제 긴급 건을 썼다. 기존 페이지가 GPT 생성 이미지의 색상·로고 불일치로 재작업이 필요한 상태이고, 렌탈 표시 의무(총액·의무기간·위약금·회수비)라는 실제 규제 판단이 들어가 사람 개입 지점을 보여주기에도 맞다.

## D10. V1을 고치지 않고 새 폴더로

V1(`상세페이지자동화`)은 지금도 업무에 쓰이고 있고, 견적·거래처 메모 같은 업무 자료가 같은 트리에 있다. 공개 저장소로 올릴 것을 고려해 **분리된 폴더 + 분리된 git**으로 시작했다. 플레이북은 사본으로 가져왔다(`knowledge/playbook.md`).

## D11. 인계 후 완료 판정은 실제 파일과 사용자 검수로

2026-09-09: SDK ResultMessage의 오류와 필수 파일 누락을 서버가 확인하며, 정상 종료만으로 done 처리하지 않는다. 파일·규칙 검사를 통과하면 최종 검수 질문을 저장하고 사용자 답변을 기다린다. 질문 답변은 이벤트 전달 전에 디스크에 저장해 중복 재실행을 막는다. 기존 실행 증거는 보존하고 평가 결과는 별도 스냅샷으로 만든다. 현재 공개 없이 로컬 데모만 준비하며, 사용자 재확인 마감은 17:30 KST다.
