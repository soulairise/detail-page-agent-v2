# PRD — 소울매트 상세페이지 에이전트 V2

> 버전 v2.0.0 · 작성 2026-09-09 · 작성자 소울매트(김민영)
> 과제: AI Agent 파헤치기 1기 Main Quest 4 — 내 도메인에서 사용할 에이전틱 워크플로 서비스
> 전작: `소울매트업무/상세페이지자동화` (V1, 2026-08~09). V1과의 차이는 §10.

---

## 1. 문제 정의

### 지금 무슨 일이 반복되는가

소울매트는 1688에서 요가·명상 용품을 소싱해 네이버 스마트스토어와 쿠팡에 등록한다. 상품 하나를 올리려면 매번 같은 일을 한다.

1. 네이버·쿠팡에서 경쟁 상품 10~20개를 훑고 가격대·상품명 패턴·리뷰 불만을 메모한다
2. 우리 상품을 어떤 각도로 팔지(타겟·포지셔닝) 정한다
3. 상세페이지 섹션 8~12개의 순서와 메시지를 짠다
4. 섹션별 카피를 쓴다
5. **금칙어·규제 검사**를 한다 — 의료 효능 표현, 근거 없는 최상급, 렌탈 표시 의무, 상품정보제공고시 누락
6. 이미지 컷별 프롬프트를 쓰고 생성한다
7. 860px 규격 HTML로 조립한다
8. 등록 항목(상품명 50자, 태그 10개, 최하위 카테고리, 고시정보 12단)을 뽑는다

상품 1개에 반나절~2일. 월 3~5개를 올리므로 이 작업이 등록 속도의 병목이다. 특히 5번은 빠뜨리면 판매 정지·행정처분으로 이어지는데, 사람이 매번 눈으로 훑는 방식이라 놓치기 쉽다.

### 에이전트가 끝내주는 것

| | 내용 |
|---|---|
| **입력** | 상품명·1688 실측 스펙·판매 유형(B2C/B2B문의/규제민감)·참고 링크·사진(선택) |
| **에이전트가 하는 일** | 시장 조사 → 포지셔닝 후보 제안 → 심층 조사 → 스토리보드 설계 → 카피 작성 → 컴플라이언스 검사·자동 교정 → HTML 조립 → 등록 항목 산출 |
| **사람이 하는 일** | 포지셔닝 선택, 스토리보드 승인, 경고 확인, 이미지 생성 승인(비용), 최종 승인 |
| **결과물** | `detail-page.html`(860px 규격) + `listing.json`(상품명·태그·카테고리·고시정보) + `image-prompts.json` + `plan.md`(기획서) + 실행 로그, ZIP 한 개 |

**완료 정의**: 사람이 손대지 않고 그대로 스마트스토어 등록창에 붙여 넣을 수 있는 상태. 금칙어 검사 통과가 완료의 필수 조건이다.

## 2. 타겟 유저

| 유저 | 상황 | 이 서비스에서 얻는 것 |
|---|---|---|
| **1차: 소울매트 대표(본인)** | 1인 운영. 소싱·통관·CS·등록을 혼자 한다. 판단은 직접 하되 손작업은 줄이고 싶다 | 판단 지점만 남기고 조사·작성·검사를 위임 |
| 2차: 소울매트 외주 편집자 | 플레이북 기준을 모른 채 페이지를 만들면 규격이 흔들린다 | 플레이북이 코드로 강제되는 제작 환경 |
| 3차(장래): 소규모 요가용품 셀러 | 같은 규제·같은 채널, 같은 병목 | 도메인 규칙이 내장된 상세페이지 생성 서비스 |

**설계에 반영된 유저 특성**: 등록 규모가 월 3~5개로 작다. 그래서 "완전 자동 대량 생성"이 아니라 **품질 우선 + 사람 판단 보존**이 목표다. 사람이 개입할 곳을 없애는 게 아니라, 개입할 가치가 있는 곳만 남긴다.

## 3. 워크플로 설계

7단계. 각 단계에서 **에이전트가 판단할 일**과 **도구가 처리할 일**을 나눈다.

```
[1] 입력·넓은 조사 → [2] 포지셔닝 선택(사람) → [3] 심층 조사·편집 판단
 → [4] 스토리보드 승인(사람) → [5] 카피 작성·컴플라이언스 검사
 → [6] HTML 조립·이미지 프롬프트 → [7] 검수·부분 수정·내보내기(사람)
```

| 단계 | 에이전트의 판단 | 도구가 처리 | 사람 |
|---|---|---|---|
| **1. 입력·넓은 조사** | 어떤 검색어로 시장을 볼지, 자료가 충분한지 | `search_market` (채널·기간 지정 웹 검색), `read_playbook` | 상품 정보 입력 |
| **2. 포지셔닝 선택** | 서로 다른 포지셔닝 후보 2~3개와 각각의 근거·위험을 만든다 | `ask_user` | **선택** (여기서 이후 범위가 정해짐) |
| **3. 심층 조사·편집 판단** | 선택된 각도로 무엇을 더 확인해야 하는지. 결과가 크게 갈릴 때만 추가 질문 | `search_market`, `read_playbook` | 필요 시에만 답변 |
| **4. 스토리보드 승인** | 섹션 8~12개의 역할·헤드라인·핵심문장·근거·이미지 계획 | `save_artifact` | **승인 또는 수정 요청** |
| **5. 카피·컴플라이언스** | 승인된 설계대로 본문 작성. 위반 지적을 받으면 표현을 바꿔 다시 검사 | `check_compliance` (결정론적 검사), `save_artifact` | 경고 확인 |
| **6. 조립·이미지** | 규격에 맞는 HTML 구성, 컷별 프롬프트 설계 | `save_artifact`, `generate_image`(승인 필요) | **이미지 생성 승인**(비용 발생) |
| **7. 검수·내보내기** | 수정 요청을 받은 섹션만 다시 쓰고 나머지는 유지 | `check_compliance`, `save_artifact` | **최종 승인·부분 수정 지시** |

### 이미 답한 것은 다시 묻지 않는다

입력 폼에서 타겟 고객·판매 유형을 이미 받았다면 2단계에서 그 질문을 반복하지 않는다. 질문은 **결과가 크게 달라지는 지점**에서만 만든다. (예: "렌탈 총액을 표에 노출할까, 견적 문의로 유도할까" — 페이지 구조 자체가 바뀌므로 물어야 한다.)

## 4. 도구 계획

6개 도구. 각각 입력·출력 스키마와 권한, 실패 처리를 정한다.

### 4.1 도구 목록

| 도구 | 역할 | 권한 | 승인 |
|---|---|---|---|
| `search_market` | 네이버·쿠팡·웹 시장 조사 | 읽기(외부) | 자동 |
| `read_playbook` | 소울매트 플레이북·금칙어·SEO 규격 조회 | 읽기(로컬, 지정 파일만) | 자동 |
| `check_compliance` | 금칙어·규제·고시항목 결정론적 검사 | 읽기(순수 함수) | 자동 |
| `save_artifact` | 산출물 파일 생성 | 쓰기(**해당 실행 폴더 내부로 제한**) | 자동 + 로그 |
| `generate_image` | OpenAI 이미지 생성 | 외부 API 호출(**과금**) | **사람 승인 필수** |
| `ask_user` | 웹 화면으로 질문하고 답 대기 | 없음(중단) | — |

### 4.2 스키마

```jsonc
// search_market — 시장 조사
入 { "query": "요가매트 렌탈 행사", "channel": "naver|coupang|web", "days": 90, "max_results": 10 }
出 { "as_of": "2026-09-09T15:00+09:00", "period_days": 90,
     "results": [{"title","url","price","published_at","snippet"}],
     "insufficient": false }

// read_playbook — 도메인 지식 조회
入 { "section": "forbidden_words|seo_spec|listing_notice|rental_rules|image_rules" }
出 { "section": "...", "content": "...", "source": "플레이북_요가용품_2026-08.md#2-6" }

// check_compliance — 규정 검사 (결정론적, LLM 아님)
入 { "text": "...", "profile": "b2c-goods|b2b-inquiry|regulated", "scope": "section|full_page" }
出 { "passed": false,
     "violations": [{"rule_id":"MED-001","severity":"block|warn",
                     "matched":"교정","span":[12,14],
                     "why":"의료적 효능 표현 — 화장품법·의료기기법 광고 제한",
                     "suggest":"자세를 편하게 잡도록 돕는"}],
     "missing_required": ["상품정보제공고시: 제조자", "렌탈: 위약금 기준"] }

// save_artifact — 산출물 저장
入 { "path": "detail-page.html", "content": "...", "kind": "html|json|md" }
出 { "ok": true, "abs_path": "runs/mat-rental/artifacts/detail-page.html", "bytes": 48213 }
   // path는 실행 폴더 기준 상대경로만 허용. ../ 및 절대경로는 거부.

// generate_image — 이미지 생성 (과금·승인 필요)
入 { "cut_id": "01_hero", "prompt": "...", "size": "1024x1536", "n": 1, "refs": ["ref1.jpg"] }
出 { "ok": true, "files": ["images/01_hero.png"], "model": "gpt-image-1.5",
     "cost_usd": 0.19, "ai_generated_notice_required": true }

// ask_user — 사람에게 질문
入 { "question": "...", "options": [{"label","description"}], "multi_select": false,
     "question_id": "positioning-001" }
出 { "answer": ["행사·기업 웰니스 중심"], "answered_at": "..." }
```

### 4.3 실패 처리 규칙

| 상황 | 처리 |
|---|---|
| `search_market` 0건 또는 타임아웃 | 1회 재시도 → 기간을 넓힐지 사용자에게 확인 → 그래도 없으면 `insufficient: true`로 진행하되 **결과물에 "시장 근거 미확보" 표시** |
| `check_compliance` block 위반 | 에이전트가 표현을 고쳐 재검사. **최대 3회**. 3회 초과 시 해당 섹션을 `needs_human`으로 표시하고 진행 중단 |
| `save_artifact` 경로 위반 | 즉시 거부하고 에러 반환. 재시도 없음. trace에 보안 이벤트로 기록 |
| `generate_image` 실패(한도·인증) | 원인을 화면에 표시. 이미지 없이 placeholder로 페이지 완성 가능 — 전체를 막지 않는다 |
| 엔진 응답이 규약 위반(JSON 깨짐) | 1회 재요청 → 실패 시 `waiting_for_user`로 남기고 원문을 trace에 보존. **완료로 처리하지 않는다** |
| 서버 재시작 | 저장된 state로 중단 지점 복원. 대기 중이던 질문을 다시 보여줌 |

### 4.4 권한 최소화

- 쓰기는 `runs/<product>/` **아래로만**. 경로 정규화 후 접두사 검사, 심볼릭 링크 거부
- 발행(스마트스토어·쿠팡 API 등록)은 **MVP 범위에서 제외**. 되돌리기 어려운 작업은 손으로 붙여넣게 둔다
- 과금 도구(`generate_image`)는 사람 승인 없이 호출 불가. 승인 시에도 컷 수·예상 비용을 먼저 표시
- API 키는 `.env`(권한 600, `.gitignore`) — 저장소에 올리지 않는다

## 5. 사람 개입 지점 (Human-in-the-loop)

| # | 지점 | 왜 사람이어야 하는가 | 막는 것 |
|---|---|---|---|
| H1 | **포지셔닝 선택**(2단계) | 이후 모든 작업의 범위를 정한다. 사업 판단이 필요 | 엉뚱한 각도로 전체 페이지를 낭비 |
| H2 | **스토리보드 승인**(4단계) | 카피를 쓰기 전 구조를 고치는 게 훨씬 싸다 | 다 쓴 뒤 전면 재작업 |
| H3 | **컴플라이언스 경고 확인**(5단계) | 자동 교정이 의미를 훼손했을 수 있다 | 규정은 지켰지만 뜻이 틀린 문장 |
| H4 | **이미지 생성 승인**(6단계) | 과금·비가역 | 예상 못 한 비용 |
| H5 | **최종 승인·부분 수정**(7단계) | 등록은 사람 책임 | 검증 안 된 페이지의 실제 등록 |

**규칙**: 게이트 통과 버튼은 사람만 누른다. 에이전트는 `waiting_for_user`로 멈출 뿐 스스로 승인하지 않는다. (V1에서 확립한 규칙을 그대로 승계)

## 6. 관찰 가능성 (Observability)

화면에서 확인 가능해야 하는 것:

- **실행 트레이스**: 턴마다 `도구명 / 입력 요약 / 출력 요약 / 성공·실패 / 소요 ms`. 펼침·접힘 가능하고, 접어도 기록은 쌓인다
- **단계 보드**: 7단계 중 현재 위치, 각 단계 상태 (`locked / running / waiting_for_user / done / failed`)
- **비용·시간**: 실행별 누적 토큰·예상 비용(USD)·경과 시간. 단계별로도 표시해 병목을 본다
- **결과물 확인**: "끝났다"는 말이 아니라 실제 파일. HTML 미리보기 iframe + 파일 목록 + 다운로드
- **컴플라이언스 리포트**: 검사한 규칙 수, 위반 → 교정 이력 (초안 표시를 검증 완료로 읽지 않도록 구분 표기)

저장 위치: `runs/<product>/trace.jsonl` (append-only), `runs/<product>/state.json` (현재 상태 단일 원본).

## 7. 에이전트 루프와 종료 조건

```
목표 수신 → 계획 → 도구 호출 → 결과 관찰 → 다음 행동 결정 → (반복)
                        ↓ 질문이 필요하면
                  waiting_for_user 로 저장하고 중단 → 답변 수신 → 같은 세션으로 재개
```

- **엔진**: Claude Agent SDK (Python). `query()` + `can_use_tool` 콜백으로 도구 승인·질문을 웹으로 넘긴다. 세션 ID를 저장해 `resume`으로 이어간다
- **상태 관리**: `state.json`이 단일 원본. 프로젝트 ID, 세션 ID, 현재 단계, 대기 질문(`question_id`·`version`), 사용자 답변, 지금까지 만든 원고, 산출물 경로
- **중복 방지**: 답변은 `question_id + version`으로 멱등 처리. 같은 답을 두 번 보내도 한 번만 실행
- **종료 조건**: 최대 턴 40 · 최대 비용 $2.00/실행 · 최대 작업시간 25분(사람 대기 제외) · 연속 실패 3회. 어느 하나에 걸리면 `halted`로 멈추고 사유를 화면에 표시한다

## 8. MVP 범위와 화면

### 반드시 있어야 하는 것 (오늘)

- [ ] 상품 입력 폼 → 실행 생성
- [ ] 1~7단계 에이전트 루프 (실제 Claude Agent SDK 호출)
- [ ] 도구 6개 중 최소 4개 실제 동작 (`search_market`, `read_playbook`, `check_compliance`, `save_artifact`)
- [ ] 사람 개입 H1·H2·H5 웹 화면
- [ ] 실행 트레이스 화면 + 비용·시간
- [ ] 새로고침·서버 재시작 후 재개
- [ ] 요가매트 렌탈 1건 end-to-end 완주 + 산출물 다운로드
- [ ] 평가 3시나리오 표

### 이번에 안 하는 것

- 스마트스토어·쿠팡 API 자동 발행 (V1 판단 유지: 월 20개 초과 시점으로 미룸)
- 회원 인증·과금·멀티 유저
- 이미지 실제 생성 자동화 (승인 UI까지만, 생성은 기존 `gen_image.py` 수동 실행 경로 유지)

### 화면

| 화면 | 내용 |
|---|---|
| `/` 제품 목록 | 실행 목록·상태·새 실행 만들기 |
| `/run/<id>` 실행 보드 | 좌: 7단계 진행 / 중: 현재 카드(질문·승인·결과) / 우: 트레이스·비용 |
| 결과 패널 | HTML 미리보기, `listing.json`, 컴플라이언스 리포트, ZIP 다운로드 |

## 9. 평가 계획

시나리오와 지표를 미리 정한다. 결과는 `EVAL.md`.

| # | 시나리오 | 확인할 것 |
|---|---|---|
| S1 | 요가매트 렌탈 (B2B 문의형) | 정상 완주. 렌탈 표시 의무 항목이 빠지지 않는가 |
| S2 | 네티팟 (규제 민감) | 의료 효능 표현을 `check_compliance`가 잡고 교정하는가 |
| S3 | 자료 부족 입력 (스펙 3줄만) | 지어내지 않고 부족을 표시하는가 |

지표: 완주율 · block 위반 잔존 수(0이어야 함) · 사람 개입 횟수 · 소요 시간 · 비용 · 재시도 횟수.

**수용 기준**: S1~S3 중 2건 이상이 사람 개입 5회 이하로 산출물까지 도달하고, 완성된 페이지에 block 등급 위반이 0건.

## 10. V1과 무엇이 다른가

| | V1 (상세페이지자동화) | V2 (이 문서) |
|---|---|---|
| 엔진 | **사람(클로드 세션)이 state.json을 손으로 채움** | 앱이 Claude Agent SDK를 호출하는 자동 루프 |
| 도구 | 없음 (사람이 판단·검색) | 스키마로 정의된 6개 도구 |
| 금칙어 검사 | 사람이 눈으로 확인 | `check_compliance` 결정론적 검사 + 자동 교정 |
| 로그 | 없음 | `trace.jsonl` + 화면 트레이스·비용 |
| 재개 | 없음 | 세션 ID·상태 저장으로 재개 |
| 승인 | 사람만 (유지) | 사람만 (유지) |

승계하는 것: 6단계 게이트 사상, `state.json` 단일 원본, 플레이북·금칙어 규칙, 3층 프롬프트 구조, 승인은 사람만.

## 11. 배포

- **로컬 실서비스**: `python -m app.server` → `http://127.0.0.1:8765`. 실제 에이전트 실행은 여기서 한다
- **공개 URL**: GitHub Pages에 체험용 페이지 배포. 실제 실행 트레이스·질문·산출물을 담아 링크만으로 흐름을 볼 수 있게 한다. 실행 엔진이 붙지 않는 범위를 README에 명시한다
- 키는 환경변수(`ANTHROPIC_API_KEY`, `OPENAI_API_KEY`), 실행 방법은 README

---

### 미정 항목

| 항목 | 상태 |
|---|---|
| 이미지 생성 실제 연결 시점 | 승인 UI까지 MVP, 실제 호출은 마감 후 |
| 다중 상품 동시 실행 | 지금은 순차 1건 |
| 발행 API | 보류 (V1 판단 유지) |

## 2026-09-09 인계 후 구현 현황

마감은 사용자 확인으로 17:45 KST로 조정. 실제 수용 기준은 아직 미충족이며 EVAL.md를 참조한다. 최종 검수 게이트는 서버에서 강제하고, H1·H2 단계 순서는 아직 프롬프트 의존이다. 비용·시간 제한의 누적 집계 및 무응답 중단 한계는 HANDOFF.md에 기록했다.

## 12. 채널별 최종 출력과 이미지 검수 — 사용자 요청 2026-09-09

### 출력 대상

- 카페24 자사몰: 상세페이지 HTML. 반응형 최대 폭 860px, 외부 스크립트 없이 본문 삽입 가능한 형태. 이미지 원본과 업로드 경로 매핑을 함께 제공한다. 로컬 주소를 운영 HTML에 넣지 않는다.
- 네이버 스마트스토어·쿠팡: JPEG 여러 장. **모든 파일 가로 정확히 860px, 세로는 각 파트의 주제·분량에 따라 1200~3000px**로 결정한다. 일정 높이에서 텍스트·표·사진을 기계적으로 자르지 않는다.
- 파일명: `01_소울매트_요가매트렌탈_행사와기업웰니스_서비스소개.jpeg`처럼 `두 자리 순번_전체 상세페이지명_파트별 상세 설명`을 사용한다. 정렬 순서가 페이지 순서다.
- 채널별 파일 목록, 이미지 크기, 생성/실사 구분, 승인 상태를 manifest에 기록하고 최종 ZIP으로 묶는다.

### 이미지 생성·검수 순서

1. 사용자 소유 OpenAI API 키로 필요한 장면 이미지를 생성한다. 키를 코드·프롬프트·로그·GitHub에 넣지 않는다.
2. 생성된 이미지를 사용자에게 보여주고 각 이미지의 사용 승인 또는 수정 요청을 받는다.
3. **사용자가 검증하기 전에는 생성 이미지를 상세페이지에 넣거나 최종 출력본으로 확정하지 않는다.** 레이아웃·문구·출력 도구 준비는 별도로 진행한다.
4. 승인된 이미지만 넣고 HTML·분할 JPEG를 생성한다. 주제별 가독성, 여백, 자름, JPEG 크기·파일명과 채널용 경로를 확인한다.
5. 최종 결과물의 사용자 검수 후 제출용 GitHub에 실제 화면 캡처와 함께 반영한다. 과제 제출 자체도 사용자 확인 후 진행한다.

### 실물과 연출 이미지 구분

- 실물 증거(정확한 색상표, 두께 측정, 실제 세척·포장 상태)는 AI로 꾸미지 않는다. 확보된 원본 사진을 검토해 사용하거나 실제 사진을 요청한다.
- 행사장·기업 웰니스·스튜디오 장면은 활용 예시로 생성 가능하다. AI 연출 이미지임을 표시하고 실제 납품 실적·재고·사양 증거처럼 설명하지 않는다.
- 기존 자료에는 렌탈 재고 5mm와 신규 소싱 6mm, 색상·위생 프로세스의 확인 상태가 충돌한다. 현재 재고에 대한 사용자 확인 전에는 해당 값을 확정하지 않는다.
- 구형 규격(한 장 최대 5000px, HTML만 산출)은 이번 사용자 규격으로 대체한다. 대표 썸네일 생성은 이번 요청 범위에 포함하지 않는다.

### 완료 기준

승인된 이미지 사용, 카페24 HTML 1종 이상, 주제별 JPEG 복수 장(860×1200~3000), 파일명·실제 픽셀 크기 검사 통과, 최종 사용자 검수. 이 조건 전에는 '최종 완료'가 아니라 검토 중으로 표시한다.
